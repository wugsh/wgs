#include <stdio.h>
#include <math.h>

int main()
{
	int n = 3, a[3];
	double s;

//	printf("数的个数n=");
//	scanf("%d\n",&n);
	
//	for(int i = 0; i < n; i++)

//scanf("%d",&a[i]);
	while(1)
	{
		scanf();	
	}
	for(int i = n-1; i < 0; i--)
	{
		s = sqrt((double)a[i]);
		printf("s = %f ",s);
	}
	return 0;
}
